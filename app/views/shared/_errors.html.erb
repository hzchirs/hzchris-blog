<% if @post.errors.any? %>
  <div class="errors">
    <ul class="callout alert">
      <% @post.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>
